# Poetry + pyproject.toml

Start
```
poetry init   # existing code
poetry new    # from scratch
```

Install
```
poetry install
```

Dependencies
```
poetry add pandas numpy 
poetry add --group dev pytest
```

Build
```
poetry build
```
